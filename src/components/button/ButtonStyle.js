import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        margin: 10,
        backgroundColor: "red",
        borderRadius: 10,
        alignItems: "center",
        width: "40%",
        justifyContent: "center"
    },
    title: {
        fontWeight: "bold",
        fontSize: 18,
        color: "white",
        marginHorizontal: 5
    },
    row: {
        flexDirection: "row"
    }
})